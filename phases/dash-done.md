# Done (Dash)

Shipped dashes. Historical reference only.

## Rules
- Do NOT modify anything here
- Use for context: `grep -rl "systems:.*auth" dashes/done/`
- Contracts in done dashes document interfaces that shipped — good grep targets
- If something needs fixing, create a new epic in backlog/ or a new dash
